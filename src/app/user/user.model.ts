// type aliases
// type User = {
//   id: string;
//   avatar: string;
//   name: string;
// };

// if datatype is object => can also use interfaces
export interface User{
  id: string;
  avatar: string;
  name: string;
};